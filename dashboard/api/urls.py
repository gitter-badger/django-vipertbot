from django.conf.urls import url, include

# from .views import (
#     # UserDetailApiView,
#     # UserListApiView,
#     # UserCreateApiView,
#     # UserUpdateApiView,
#     # UserDeleteApiView
# )

urlpatterns = [
    # url(r'^$', UserListApiView.as_view(), name='users-list'),
    # url(r'^create/$', UserCreateApiView.as_view(), name='users-create'),
    # url(r'^(?P<id>\d+)/$', UserDetailApiView.as_view(), name='users-detail'),
    # url(r'^(?P<id>\d+)/edit/$', UserUpdateApiView.as_view(), name='users-update'),
    # url(r'^(?P<id>\d+)/delete/$', UserDeleteApiView.as_view(), name='users-delete'),
]
